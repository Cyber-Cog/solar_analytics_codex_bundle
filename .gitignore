# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.pycache

# Environment — never commit credentials
.env
*.env
.env.local
.env.*.local
!.env.example

# Node (vanilla frontend has no build tooling, but ignore stray node deps anywhere)
node_modules/

# Retired Next.js tree — app UI lives under frontend/ only
frontend-next/

# Databases & dumps
*.db
*.sql
*.dump
*.bak

# Archives
*.zip
*.7z
*.tar
*.tar.gz
*.tgz

# Logs & runtime output
logs/
backend/logs/
*.log
output*.txt
server_log.txt
uvicorn_log.txt

# Scratch / temp working folders (never commit raw data exports)
tmp/
tmp_*/
scratch/
*.xlsx
*.xls
*.csv
# ...but keep shipped templates if any live under backend/common/templates.py code (not under tmp/)

# Uploaded spec sheets and user uploads (keep folder structure only)
backend/uploads/spec_sheets/*/*
!backend/uploads/spec_sheets/.gitkeep

# Local backups (user-configurable, never inside repo)
backups/

# OS
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/

# Vercel build output
public/
